name: Block Invalid Release Branches

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - master

jobs:
  block-invalid-release-pr:
    uses: salva133/My-Workflows/.github/workflows/block-invalid-release-branches.yml@a7b34cd4067123ab5623d7caadbe45f7cf8e8fa4
    with:
      head_ref: ${{ github.head_ref }}
